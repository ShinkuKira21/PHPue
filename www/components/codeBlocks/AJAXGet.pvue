<template>
<pre class="text-green-400 text-sm"><code>@AJAX('GET')
function updateSession() {
    if(isset($_GET['token'])) {
        $_SESSION['userToken'] = $_GET['token'];
    }
}</code></pre>
</template>